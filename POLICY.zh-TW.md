# 公開倉庫政策

**版本**: 1.0  
**日期**: 2026-08-20  
**狀態**: 生效中  

---

## 版權聲明

本文件及本倉庫內所有由專案貢獻者原創之內容，均受著作權法保護。  
除依本政策明確允許之用途外，禁止任何未經授權之重製、散布、修改或商業使用。  

---

## 1. 目的

此倉庫為**概念展示與公開記憶層**，僅用於說明系統架構、研究方向與可驗證之性能數據。  
**禁止**將任何敏感、可執行或可還原核心實作之資料放入此倉庫。

---

## 2. 允許之內容

以下內容**可以**出現在本倉庫：

- `README.md`：專案概述、架構圖、API 概念說明。
- `docs/`：架構文件、資料流圖、模組能力表（使用中性命名）。
- `benchmark/`：模擬測試結果、延遲數據、準確率指標（必須標註 `SIMULATION` 或 `PROTOTYPE`）。
- `examples/`：公開介面之輸入／輸出範例（不含真實金鑰或完整業務邏輯）。
- `tests/`：公開測試用例（僅測試公開介面，不包含私有依賴）。
- `LICENSE`：開源授權條款（如適用）。
- `SECURITY.md`：安全政策與回報流程。

**所有 Markdown 文件不得包含內部 Bot/AI 具體名稱、私有 IP、域名、金鑰、權重路徑或完整演算法描述。**

---

## 3. 禁止之內容

以下內容**絕對禁止**出現在本倉庫：

- 壓縮檔案：`.zip`, `.7z`, `.rar`, `.tar`, `.gz`, `.bz2`, `.xz`
- 原始碼檔案：`.py`, `.js`, `.ts`, `.jsx`, `.tsx`, `.sh`, `.ps1`, `.bat`, `.cmd`
- 設定檔：`.yml`, `.yaml`, `.json`, `.env`, `.ini`, `.toml`
- 可執行檔／二進位檔：`.exe`, `.dll`, `.so`, `.dylib`, `.bin`, `.iso`
- 金鑰與憑證：`.pem`, `.key`, `.crt`, `.pfx`
- 版本控制元資料：`.git/`, `.github/`, `.gitlab/`, `.gitignore`（僅可保留最小 `.gitignore` 排除垃圾檔案）
- CI/CD 工作流程：`.github/workflows/`, `.gitlab-ci.yml`
- 任何包含內部命名（如特定 Bot 代號、模組代號）之文件
- 任何未標記為 `SIMULATION` 的測試數據
- 任何可能暴露私有倉庫結構、內部 IP、域名或憑證之資訊

**違規檔案將被公開層防火牆自動攔截，或由管理員立即移除。**

---

## 4. 程式碼可見性政策

本倉庫採用 **公開外殼 + 私有核心** 架構：

- **公開倉庫**：只包含文件、介面定義、範例與測試。
- **私有倉庫**：包含核心實作、模型權重、Router、Memory、Agent、Prompt 與金鑰。
- 公開程式碼中引用核心模組時，必須使用抽象介面或佔位符，不得直接暴露私有依賴。
- 私有倉庫存取權限僅授予：
  - 核心開發團隊
  - 與使用者合作之 AI 助手（透過 fine-grained personal access token 唯讀授權）
- 公開倉庫的 CI 檢查器（放置於私有管理庫）會在推送前驗證檔案類型與敏感模式。

---

## 5. 執行方式

- 透過 `public_channel_guard.py` 掃描檔案類型與路徑。
- 透過 GitHub Actions（僅存在於私有庫）在合併前檢查。
- 人工審核所有 Markdown，確保無內部命名洩露。

違反本政策之提交將被拒絕，並記錄稽核日誌。

---

## 6. 重要聲明

此倉庫為**概念參考**，不構成可執行產品、專利或商業承諾。  
任何實際部署均需獨立工程與安全認證。

---

**政策結束**
