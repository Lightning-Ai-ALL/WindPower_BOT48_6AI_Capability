# .github/workflows/public_guard.yml

name: Public Channel Guard

on:
  push:
  pull_request:

jobs:
  guard:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Check public repository
        run: |
          python public_channel_guard.py
